services:
  - type: web
    name: spotify-song-extractor
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: FLASK_ENV
        value: production
    disk:
      name: data
      mountPath: /output_songs
      sizeGB: 1
